import { IUserModel } from '@/types/user';
import React from 'react';

interface IProps {
  user?: IUserModel;
}

const CreatePin: React.FC<IProps> = () => {
  return <div>CreatePin</div>;
};

export default CreatePin;
