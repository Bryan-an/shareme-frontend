import { IUserModel } from '@/types/user';
import React from 'react';

interface IProps {
  user?: IUserModel;
}

const PinDetail: React.FC<IProps> = () => {
  return <div>PinDetail</div>;
};

export default PinDetail;
